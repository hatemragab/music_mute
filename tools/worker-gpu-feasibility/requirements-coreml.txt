# B2 direct pins proven on the real M4 Pro host. The lock file freezes transitives.
audio-separator[cpu]==0.47.0
# audio-separator 0.47.0 imports audioread but omits it from package metadata.
audioread==3.1.0
onnxruntime==1.30.0
