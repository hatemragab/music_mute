# Isolated integration fixture only; the application uses external Redis.
bind 127.0.0.1
port 6379
protected-mode yes
dir /data
appendonly yes
appendfsync always
maxmemory 512mb
maxmemory-policy noeviction
save 900 1
save 300 10
